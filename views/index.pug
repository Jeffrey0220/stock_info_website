extends layout.pug 

block main
                h1 STOCKS ONLINE
                
                form(action="/search" methon="GET")
                        div.mb-3
                                label.form-label Explore stocks infomation and news
                                input.form-control(type="text" placeholder="Enter a stock symbol" name="searchterm" size="50" required)
                        button.btn.btn-primary(type="submit") SEARCH
                
                
                h6(style="position: fixed; bottom:70px; right:50px; font-weight:bold;") VISITOR COUNTER: #{count}
  




